# Advancement A1 — Layout Detection

> Advancement brief. Part of the [Build Roadmap](../../plan.md). Status: **Not started**. Begins only once the core (Phases 0–5) is green.

**Goal:** Add document-region structure ahead of OCR/KIE.

## Research 🔬
- [ ] DocLayout-YOLO weights/license; ONNX export + CPU latency.
- [ ] Does layout improve downstream KIE? (spike)

## Tasks
- [ ] `LayoutDetector` interface + DocLayout-YOLO (ONNX Runtime).
- [ ] Integrate: preprocess → layout → region-scoped OCR → KIE.
- [ ] Visual debug output (boxes overlaid).

## Done when 📦
- [ ] `/extract` returns labeled regions; layout runs as ONNX on CPU within budget.

## Report
On completion, add `report_a1.md` to this folder.
