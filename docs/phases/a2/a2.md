# Advancement A2 — GraphRAG over Extracted Data + `/ask`

> Advancement brief. Part of the [Build Roadmap](../../plan.md). Status: **Not started**. Begins only once the core (Phases 0–5) is green.

**Goal:** Answer natural-language questions grounded in extracted documents.

## Research 🔬
- [ ] Embedding model on CPU (quality vs latency).
- [ ] Graph/chunking strategy for structured docs; RAGAS eval set.

## Tasks
- [ ] Build a graph/vector index over extracted JSON (with doc/field metadata).
- [ ] Retriever + prompt assembly; `POST /ask` → grounded answer + citations.
- [ ] RAGAS evaluation (faithfulness, answer relevancy).

## Done when 📦
- [ ] `/ask` answers over indexed documents with citations; RAGAS scores recorded.

## Report
On completion, add `report_a2.md` to this folder.
